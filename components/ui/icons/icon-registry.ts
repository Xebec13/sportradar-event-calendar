// Single source of truth for all Lucide icons in the project.
// To add an icon: 1) import it here, 2) add it to iconRegistry.
// IconName type in Icons.tsx is derived automatically — no manual updates needed.

import { ArrowLeft, Calendar, CalendarPlus, CalendarDays, ChevronLeft, ChevronRight, Circle, Square } from "lucide-react"

export const iconRegistry = {
    ArrowLeft,
    Calendar,
    CalendarPlus,
    CalendarDays,
    ChevronLeft,
    ChevronRight,
    Circle,
    Square,
} as const
